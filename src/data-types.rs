// Run this code once in debug mode
// and once in release mode.
// What is the difference? Why is it happening?
//
// Change the code to get the right result for the sum.
fn main() {
    let foo : u8 = 242;
    let bar : u8 = 20;

    let result = foo + bar;

    println!("{} + {} = {}", foo, bar, result);
}
